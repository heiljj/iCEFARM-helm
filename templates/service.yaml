apiVersion: v1
kind: Service
metadata:
  name: {{ include "iCEFARM.fullname" . }}
  labels:
    {{- include "iCEFARM.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iCEFARM.selectorLabels" . | nindent 4 }}
