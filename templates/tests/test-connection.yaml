apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iCEFARM.fullname" . }}-test-connection"
  labels:
    {{- include "iCEFARM.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iCEFARM.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
